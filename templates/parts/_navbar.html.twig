<nav class="navbar navbar-expand-lg bg-body-tertiary right">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path("app_default") }}">HumanBooster</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="nav navbar-nav navbar-right">
            <div class="navbar-nav">
                {% if app.user %}
                {% if app.user.hasRole('ROLE_RH') %}
                <a class="nav-link active" aria-current="page" href="{{ path("app_user_index") }}">Gestion</a>
                <a class="nav-link" href="{{ path("app_default") }}">Liste</a>
                {% endif %}
                <a class="nav-link" href="{{ path("app_logout") }}">Déconnexion</a>
                {% endif %}
            </div>
        </div>
    </div>
</nav>
