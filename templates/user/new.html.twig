{% extends 'base.html.twig' %}

{% block title %}{{title}}{% endblock %}

{% block body %}
        <div class="d-flex justify-content-between">
            <h5>Créer un nouvel utilisateur</h5>
            <a href="{{ path('app_user_index') }}" class="btn">Retour</a>
        </div>
    <div class="container">

    {{ include('user/_form.html.twig') }}

    </div>
{% endblock %}
